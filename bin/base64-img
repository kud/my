#! /usr/bin/env zsh

# take a png/jpg/gif and copy/paste it as base64 into clipboard

source $MY/core/helper

MIMETYPE=$(file -b --mime-type $argv)
IMGBASE64=$(base64 $argv)

echo "data:$MIMETYPE;base64,$IMGBASE64" | pbcopy
echo_success 'Copied in the clipboard!'
