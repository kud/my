# Claude Desktop Configuration - Home Profile
# Adds Todoist MCP server for home usage (command wrapper for reliability)

mcp:
  todoist:
    transport: stdio
    command: npx
    args:
      - -y
      - mcp-remote@latest
      - https://ai.todoist.net/mcp
