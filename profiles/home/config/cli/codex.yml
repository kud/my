# Codex CLI Configuration - Home Profile
# Adds Todoist MCP server for home usage (command wrapper for reliability)

mcp_servers:
  todoist:
    command: mise
    args:
      - exec
      - node@latest
      - --
      - npx
      - -y
      - mcp-remote@latest
      - https://ai.todoist.net/mcp
