# OpenCode Configuration - Home Profile
# Adds Todoist MCP server for home usage

mcp:
  todoist:
    type: local
    enabled: true
    command:
      - mise
      - exec
      - node@latest
      - --
      - npx
      - -y
      - mcp-remote@latest
      - https://ai.todoist.net/mcp
