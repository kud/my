################################################################################
#                                                                              #
#   🤖 MISTRAL VIBE CONFIGURATION                                               #
#   -----------------------------                                               #
#   Simple YAML config for Mistral Vibe theme and MCP servers                  #
#                                                                              #
################################################################################

# Theme configuration
theme: "terminal"

# MCP Servers configuration
mcp_servers:
  - name: "github"
    transport: "http"
    url: "https://api.githubcopilot.com/mcp"
    headers:
      Authorization: "Bearer ${MCP_GITHUB_TOKEN}"
    
  # - name: "basic-memory"
  #   transport: "stdio"
  #   command: ["uvx", "--python", "3.12", "basic-memory", "mcp"]
    
  - name: "notion"
    transport: "http"
    url: "https://mcp.notion.com/mcp"