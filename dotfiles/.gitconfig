[core]
  whitespace   = fix,-indent-with-non-tab,trailing-space,cr-at-eol
  # editor       = subl -n -w
  editor       = vim
  excludesfile = ~/.gitignore_global
[push]
  # default = tracking
  default = simple
[color]
  ui = true
[color "branch"]
  current = yellow reverse
  local   = yellow
  remote  = green
[color "diff"]
  meta       = yellow bold
  frag       = magenta bold
  old        = red bold
  new        = green bold
  whitespace = red reverse
[color "status"]
  added     = yellow
  changed   = green
  untracked = cyan
[alias]
  aa                    = add .
  ac                    = "!git add -A && git commit"
  co                    = checkout
  br                    = branch
  ci                    = commit
  st                    = status
  df                    = diff
  dc                    = diff --cached
  # diff-mdr              = "!git diff $@ | mdr"
  diff-subl             = "!git diff $@ | subl"
  diff-files-subl       = "!git diff --name-only | uniq | xargs subl"
  gh                    = "!github_cli ${@:-.}"
  lg                    = log -p
  lol                   = log --graph --decorate --pretty=oneline --abbrev-commit
  lola                  = log --graph --decorate --pretty=oneline --abbrev-commit --all
  ls                    = ls-files
  ign                   = ls-files -o -i --exclude-standard
  lgb                   = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
  unstage               = reset HEAD --
  unstash               = stash pop
  last                  = log -1 HEAD
  oops                  = commit --amend
  pt                    = "!git push && git push --tags"
  pr                    = "!hub pull-request"
  fix                   = "!git add -A && git commit --amend --no-edit"
  rmdf                  = "!git rm $(git ls-files --deleted)"
  # export                = "!git clone --depth 1 $1 && rm -rf \"$(basename $1 .git)/.git\" -"
  branches              = branch -avv
  wip                   = "!git add -A && git commit -m '🔥 WIP 🔥'"
  unwip                 = "!git reset --soft HEAD~1"
  lazy                  = "!git add -A && git commit -m '🌬️' && git push"
  refresh               = "!git fetch && git rebase origin/master"
  rewrite               = "!git rebase -i $(git merge-base master HEAD)"
  finalise              = "!git refresh && git rewrite"
  browser               = "!open $(echo $(git config --get remote.origin.url)|sed 's/:/\\//g'|sed 's/git@/https:\\/\\//g'|sed 's/\\.git//g')"
  cleanup               = "!git remote prune origin && git repack && git gc"
  my-activities         = reflog --relative-date
  show-aliases          = "!git config --get-regexp alias|sed \"s/alias.\\([a-zA-Z-]*\\) \\(\\.\\)*/$(tput setaf 2)\\1 $(tput setaf 8)\\2/\""
[branch]
  autosetuprebase = always
[include]
  path = .gitconfig_local
[github]
  user = kud
[filter "hawser"]
  clean = git hawser clean %f
  smudge = git hawser smudge %f
  required = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[fetch]
  prune = true
[ghi]
	token = !security find-internet-password -a kud -s github.com -l 'ghi token' -w
